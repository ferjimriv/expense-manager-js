'use strict';

/**
 * Entries rates controller
 * @param {mixed} $scope
 */
function RatesWidgetController($scope) {
    //$scope.entries = Entries.query();
}

angular.module('widgets').controller('RatesWidgetController', ['$scope', 'Entries', RatesWidgetController]);